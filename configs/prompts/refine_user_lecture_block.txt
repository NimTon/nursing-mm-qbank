---

**【同批「讲师提醒」】**（**本轮**须与教材修正写在**同一条** `items` JSON 里，**禁止**只写修正、另起一轮）：
每条 `items` 必须多一个键 **`讲师提醒`**（字符串）。面向学员：【核心要点】【易错点】【易混淆/辨析】【备考提示】等小节，200–600 字为宜；须结合**本题最终定稿**（若 `修正后问题` / `修正后解析` 非空则以其为准，否则以 `原问题` / `原解析`）撰写，不写与题目无关的长篇知识。
- 当 `修正状态=false` 时：`修正后问题` 等教材修正相关字段仍须全部为空串；**仅** `讲师提醒` 可非空（审题易错、概念辨析为主）。
