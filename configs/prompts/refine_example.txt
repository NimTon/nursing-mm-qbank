**输出仅允许如下 JSON 形（`items` 中每条对应一个题号；须含全部键，无可填空串）：**
{"items":[
  {
    "题号": "1",
    "原问题": "（须与本次输入一致，勿改题意）",
    "原解析": "（须与本次输入一致）",
    "修正后问题": "（若修正状态为否，这里必须为空串；若为是，这里写修正后的问题全文）",
    "修正问题原因": "（若修正状态为否，这里必须为空串；若为是，用一两句说明差异）",
    "修正问题参考来源": "（若修正状态为否，这里必须为空串；若为是，写来源）",
    "修正后解析": "（若修正状态为否，这里必须为空串；若为是，这里写修正后的解析全文）",
    "修正解析原因": "（若修正状态为否，这里必须为空串；若为是，说明差异）",
    "修正解析参考来源": "（若修正状态为否，这里必须为空串；若为是，写来源）",
    "修正状态": true
  }
]}

**`修正状态`：** 对「原问题」或「原解析」**任一处**做了有依据的实质性字句/事实修正为 `true`；**完全未改**为 `false`。
当 `修正状态=false` 时，除 `题号` / `原问题` / `原解析` 以外，其余字段（包括 `修正后问题` / `修正后解析` / 原因 / 参考来源）**必须全部输出空字符串**，不得填写「与原文一致」等说明。 