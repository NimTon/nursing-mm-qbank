__LECTURE_CONTENT_EXAMPLE__

---

**联网开关：__WEB_STATUS__。**
- 「开启」时可简要参照公开指南表述；勿杜撰书名页码。
- 「关闭」时以题面与解析为准展开。

**撰稿要求：** 分期/分型/分级类题须 **先概念与完整阶段框架，再选项逐项**；其余题型可直接锚点后逐项。须讲清含义与知识点；延伸有限度且可核验；禁止编造或与解析冲突。

**本批：** __BATCH_ID__

**待写讲课内容的题块（JSON 数组；请对每条产出一项 `items`，`题号` 必须与下表逐条一致）：**
```json
__ITEMS_JSON__
```

只输出最外层一个 JSON 对象，键为 `items`；条数与顺序与上表**完全一致**。
