# 内置提示词：当项目根下 configs/prompts/*.txt 不存在时使用（保证旧环境仍可运行）
# 发布版可通过编辑「exe 同目录/configs/prompts/」下同名文件覆盖，无需重打包

VLM_NOISE_BULLETS = """**必须整页过滤、不得出现在任何「内容」中** 的版式噪音（与试题无关，一律丢弃）包括且不限于：
- 含「启峻教育」「让梦想起飞的地方」「让梦想起飞」等培训机构标语、重复校名。
- 含「祝您考试成功」等套话。
- 书眉/页脚纯装饰页码行，如「— 153 —」这类仅居中的页码装饰。
- 报名/联系电话类：如「报名热线」「咨询热线」+ 电话数字。只要与做题无关均不得输出。"""

VLM_SYSTEM = (
    "你是专业医学/护理教材与试卷转录与分段助手。只按约定输出**且仅**输出一个 JSON 对象，不闲聊、不加 Markdown 解释。"
    "题号在卷面阅读顺序上按步长1连续递增；题号难辨时可用已辨清的上/下题号反推，勿随意跳号。"
)

VLM_JSON_EXAMPLE = """**【先遵循此 JSON 结构】** 大括号、键名 `items`、`题号` / `类型` / `内容` 必须一致。下面为**形状示例**；实际「内容」须为图中真实文字，勿照抄示例套话（图中没有的内容不要编）。

{"items":[
  {"题号": "1", "类型": "问题", "内容": "1. 下列哪项正确？\\nA. 选项A\\nB. 选项B"},
  {"题号": "1", "类型": "解析", "内容": "答案：A。因……（若无解析可填空字符串）"},
  {"题号": "2", "类型": "问题", "内容": "2. 判断题……"}
]}

**类型**只能是汉字「问题」或「解析」；**题号**优先写图中能辨认的编号。

**题号反推（重要）：** 本类试卷的题号在**阅读顺序**上**步长为 1 连续递增**（后一题 = 前一题 +1）。当某题题号**模糊、缺失或难辨**时，可根据**同一阅读顺序**上、下相邻已认清的题号来补全本段 `题号` 字段；若仅能粗判，在合理推断后写入字符串形式的题号。若**整页都无法确定任何参考题号**且无法反推，则 `题号` 可为 `""`。

---

"""

VLM_USER = VLM_JSON_EXAMPLE + f"""阅读该整页/整屏图，在排除噪音后，将**与试题相关**的可见文字按「题号 + 问题/解析」分类，输出**严格 JSON 对象**（可配合 json_object 模式），且**最外层有且仅有一个键** `items`：

- `items`：数组。每项**必须**包含三键（`题号`、`类型` 为字符串；`内容` 为字符串）：
  - `题号`：如 \"1\"、\"2\" 或与图中/反推后一致的题号。用于后处理**按题号**对应同题的「问题」与「解析」；**能根据上下题以 step=1 反推的须尽量给出**，反推的也写入此字段。仅当**无法**建立任何题号时再用空字符串 `""`。
  - `类型`：只许为 `\"问题\"` 或 `\"解析\"` 二选一。题干、选项、材料属于「问题」；考后答案讲解、易错、名师解析等属于「解析」。
  - `内容`：本段正文。勿含上述版式广告、页脚电话。无正文时 `""`。

阅读顺序：多栏时先左后右、自上而下。同一题号可出现多条 `items`（如一条「问题」+ 一条「解析」）。

{VLM_NOISE_BULLETS}
单字难辨时可用「□」占位。不要编造图中不存在的文句。除 JSON 外不要输出任何文字。"""

_LAYOUT_NOISE = (
    "输入中若出现与试题无关的**版式噪音**（如培训机构广告「启峻教育」「让梦想起飞」、"
    "「祝您考试成功」、书眉页脚「— 页码 —」、「报名/咨询热线」+电话等），拆题时**不要**写入 stem / options / analysis 等字段，当作不存在。"
)

LLM_COMPOSE_SYSTEM = (
    "你是护理考试题库编辑，输出必须是严格 JSON，不要 Markdown，不要解释性段落。"
    + _LAYOUT_NOISE
    + "题号在版面顺序上视为大家习惯的连续整数递增（+1），若整页转写中题号难辨可据上下题合理反推，勿乱编不存在的题。"
    + "若某题或解析在原文中不完整，须用字段标记，不得用常识把题目或解析补成看似完整。"
)

_LLM_EXAMPLE = """**【先遵循此 JSON 结构】** 大括号与键名须一致。下面为**形状示例**；`stem` / `options` 等值须由你根据下方**整页转写文本**实际填写，勿照抄示例字句（除非原文确同）。

{"items":[
  {"type": "单选", "stem": "1. 某患者……？", "options": ["A. …", "B. …", "C. …", "D. …"], "answer": "A", "analysis": "本页有解析时填写；本页无解析则空字符串", "confidence": 0.88, "incomplete": false, "incomplete_analysis": true, "note": ""}
]}

---

"""

_LLM_USER_TAIL = """下面是一页教材/试卷的**整页转写纯文本**（可能有错字、缺字、顺序略乱、跨页截断；若经 VLM 分类，可能已带 `【问题 n】` / `【解析 n】` 行首标签）。
**忽略** 培训机构标语、书眉页脚装饰页码行、仅「报名/咨询热线」+电话 等与做题无关的噪音，勿写入输出字段（参考启峻/梦想起飞/祝您成功 等类典型广告）。

请拆成若干道「题」，输出一个 JSON 对象，**仅含一个键** `items`，值为数组。每项须含字段（名与上例相同）：
- type: 单选|多选|判断|简答|未知
- stem: 题干（只整理原文可见内容；不臆造）
- options: 字符串数组，无选项则 []
- answer: 能确定的答案（如 A / 对），不能确定则空字符串
- analysis: 该题在本页出现的解析文字；**本页完全无解析或明显缺后半段时，必须只输出空字符串**，不得用常识或教材知识补成「完整解析」
- confidence: 0 到 1 的小数
- incomplete: 布尔。当题干或选项在原文中**不能构成完整一道题**（如跨到下一页、转写截断、题面缺失）时设为 `true`；为 `true` 时：`stem` 与 `options` 只许写**有依据的可见片段**（可部分为空/极短），**不得**为凑成一道完整题而编造、补全、续写；`answer` 无法确定时为空
- incomplete_analysis: 布尔。当本页中该题**无解析、或解析明显被截断/只出现开头**、无法从原文取全解析时设为 `true`；为 `true` 时 `analysis` **必须**是空字符串 `""`，**禁止**用课外知识把解析补全为「长段标准答案」
- note: 字符串，**可选**；有 `incomplete` 或 `incomplete_analysis` 为 true 时建议简短说明原因，如「题干跨页」「本页无解析」。若**题号**在原文中糊/缺、但**上下题题号可辨**，可依据**题号 step=1 连续递增**（后一题 = 前一题 +1）在 `stem` 中补全/理顺题头编号，并在 `note` 写如「题号据相邻题+1 推断」。

**题号规律：** 同一套卷在本页内（及与前后文顺序一致时）题号视为**等差 1、按阅读顺序递增**；在题面/转写中某题题号难读时，优先用**上下题**反推；不可无依据跳号、乱序编号。

重要：出现不完整时以标记为主，**宁可不输出看似完整的题或长解析**，也不要用模型知识伪造完整 `stem`/`analysis`；题号反推**仅**在能由相邻题号推出时使用，不得为凑整卷而虚构不存在的题。

整页原文：
---
__PAGE_PLAIN__
---
"""

LLM_COMPOSE_USER = _LLM_EXAMPLE + _LLM_USER_TAIL

REFINE_SYSTEM = """\
你是国内本科护理学教研与命题审核专家。务必以**人卫等出版社出版的最新/现行本科《护理学》及专科护理学教材、护理学综合类权威参考书与近年的护理学临床/专项指南/专家共识**为**首要核对**依据，术语、数值、护理操作要点与教材表述须一致或注明依据；在教材有歧义时以**最新再版/勘误**倾向为准。

- **联网开关见用户说明**：已开启时若当前 API 提供联网/检索，请检索「护理学 教材/指南/共识」的公开可引述来源；**未开启**时不得杜撰新书名或精确版次，可写「依据护理学通识与题面，未独立检索」等，**参考来源**中注明**局限**。
- 对「原问题/原解析」的修正限于：纠错字、去噪音残留、**医学/护理事实与规范表述**与教材/指南不一致处；**不**为凑完整而补造题干或解析不存在的原句内容。
- 只输出**一个** JSON 对象，顶层有且仅有一个键 `items`，**不要** Markdown。"""

REFINE_EXAMPLE = """**输出仅允许如下 JSON 形（`items` 中每条对应一个题号；须含全部键，无可填空串）：**
{"items":[
  {
    "题号": "1",
    "原问题": "（须与本次输入一致，勿改题意）",
    "原解析": "（须与本次输入一致）",
    "修正后问题": "（在教材依据下可订正后的问题全文，无改则同原问题）",
    "修正问题原因": "无修正则空串；有修正用一两句说明与教材/规范差异",
    "修正问题参考来源": "如：人卫《基础护理学》第×版/某指南简称；无则空或说明未联网",
    "修正后解析": "（同理）",
    "修正解析原因": "（同理）",
    "修正解析参考来源": "（同理）",
    "修正状态": true
  }
]}

**`修正状态`：** 对「原问题」或「原解析」**任一处**做了有依据的实质性字句/事实修正为 `true`，**完全**未改（仅可清理明显原稿断行/空格）为 `false`；若仅格式微调仍属未实质修正，填 `false` 并在原因中说明「与原文一致」。"""

REFINE_USER = """__REFINE_EXAMPLE__

---

**联网检索开关：__WEB_STATUS__。**
- 为「开启」时：若你所在接口**支持**联网/实时检索，可检索**近年护理学本科教材的公开题录与标准表述、权威学会指南**，并在各「参考来源」中写出**可查证的名称+版次/年份/网址线索**；若接口**实际不**带检索能力，在参考中注明「经模型知识核对，非实时检索」。
- 为「关闭」时：**不得**假称已联网；参考来源中说明依据为训练知识/题面，并避免编造具体第几版**页码**等无法核验的信息。

**本页 page_id：** __PAGE_ID__

**按题号合并后的题块（JSON 数组，请对每条产出一项 `items` 子对象）：**
```json
__MERGED_JSON__
```

只输出最外层一个 JSON 对象，键为 `items`，数组长度与上表条数**一致、顺序**与上表相同（以题号排序，与上表同序）。"""
